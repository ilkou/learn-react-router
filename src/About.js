import React from 'react';
import './App.css';

function About() {
    return (
        <div className="App">
            <h1>This is a simple exemple of using router in a react app</h1>
            <h2 style={{color: 'darkblue'}}>i'm using fortnite API to get some data and display it in the shop section, u can see more details for each item by clicking on it</h2>
        </div>
    );
}

export default About;
